---
table_title: "Auto-generated Report"
table_subtitle: "Report generated by zjleblanc.reporting.table_per_host on {{ table_timestamp }}"
table_desc: "Report generated by zjleblanc.reporting.table_per_host Ansible role"
table_timestamp: "{{ lookup('pipe', 'date +\"%Y-%m-%d @ %H:%M:%S\"') }}"
table_hosts: "{{ ansible_play_hosts }}"
# table_data_fact: table_data # tabular data fact set for each host in {{ table_hosts }}
# table_headers_fact: table_data_headers # tabular data headersfact set for each host in {{ table_hosts }}
# table_headers: ["First Name", "Last Name", "Birthday"] # optionally provide generic column headers for all hosts
table_data_type: dict # type of input data (list or object)
table_first_row_headers: false # flag for headers in the first row
table_output_dest: "{{ playbook_dir }}/zjleblanc.table_per_host.html"
# table_output_remote_host: report_server # inventory host to copy report to
table_accent_color: "#37a3a3"
